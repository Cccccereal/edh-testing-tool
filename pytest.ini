[pytest]
testpaths = tests
addopts = -m "not network"
markers =
    network: requires access to third-party sites (Scryfall, Moxfield, ...); run with `pytest -m network`, deselected by default
    ui: browser-based layout checks via Playwright (auto-skipped when playwright is not installed)
